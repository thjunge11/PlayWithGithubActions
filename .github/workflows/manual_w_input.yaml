name: Manual Run with required Input

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Your name'
        required: true        

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, ${{ github.event.inputs.name }}!"
