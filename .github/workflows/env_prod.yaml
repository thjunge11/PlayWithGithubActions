name: Environment Protected Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Simulate deploy
        run: |
          echo "This job will require environment protections (approval) before running"
          echo "If approved, deploy steps would execute here."
